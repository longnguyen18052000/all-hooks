import { FC } from "react";

const Pagination: FC = () => {
    return (
        <div>
            <button>Prev</button>
            <button>Next</button>
        </div>
    );
}

export default Pagination;